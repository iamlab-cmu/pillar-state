syntax = "proto3";

package PillarMsg;

// Uses double precision

message Property {
    repeated double values = 1;
    repeated double variances = 2;
    repeated string value_names = 3;
}

message State {
    map<string, Property> properties = 1;
}
